{
  "name": "generate-psd",
  "version": "1.0.0",
  "main": "nodes.js",
  "scripts": {
    "start": "node nodes.js",
    "build": "npm install" // ou "yarn install" dependendo do que você escolher
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
